{
  "name": "@deschutesdesigngroupllc/forumify-social-login-plugin",
  "description": "Frontend assets for the forumify social login plugin.",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "format": "prettier './templates/**/*.twig' './*.{js,json}' --write --config ./.prettierrc"
  },
  "devDependencies": {
    "@zackad/prettier-plugin-twig-melody": "^0.6.0",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8"
  }
}
